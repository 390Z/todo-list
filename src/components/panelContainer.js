const loadPanelContainer = () => {
    const panelContainer = document.createElement('div');
    panelContainer.classList.add('panel-container');

    return panelContainer;
}

export default loadPanelContainer;